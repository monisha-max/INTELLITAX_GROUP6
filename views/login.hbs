<!DOCTYPE html>
<html>
<head>
  <!-- Character encoding and viewport settings for responsiveness -->
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- Title of the web page -->
  <title>IntelliTax</title>
  <!-- Link to an external CSS stylesheet for page styling -->
  <link rel="stylesheet" href="css/login.css">
</head>
<body>
  <!-- Main container for the login interface -->
  <div class="outer">
    <!-- A text container, possibly for styling purposes -->
    <div class="carousel-text">
      <!-- Wrapper for the login form, providing additional styling -->
      <div class="wrapper">
        <!-- Form for user login -->
        <form action="/login" method="post">
          <h2>Login</h2>
          {{#if error}}
              <p>{{error}}</p>
          {{/if}}
          <!-- Input field for username -->
          <div class="input-field">
            <input type="text" name="username" placeholder="Username" required>
          </div>
          <!-- Input field for password -->
          <div class="input-field">
            <input type="password" name="password" placeholder="Password" required>
          </div>
           <!-- Placeholder for displaying wrong login information -->
          <p id="wrong" style="color: red;"></p>
           <!-- Checkbox for 'Remember me' functionality -->
          <div class="forget">
            <label for="remember">
              <input type="checkbox" id="remember">
              <p>Remember me</p>
          </div>
          <!-- Submit button for the login form -->
          <button type="submit">Log In</button>
          <div class="register">
            <p>Don't have an account? <a href="/register">Register</a></p>
          </div>
        </form>
      </div>
    </div>
  </div>
</body>
</html>

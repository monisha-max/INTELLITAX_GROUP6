<!DOCTYPE html>
<html>
<head>
    <!-- Title of the web page -->
    <title>Tax Calculation Suggestions</title>
    <!-- Internal CSS for styling the page elements -->
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            background-color: #AD88C6;
            margin: 20px;
        }

        .suggestions {
            margin: 20px;
            padding: 20px;
            background-color: #c7b7d8;
            border-radius: 10px;
        }

        .suggestions p {
            font-size: 18px;
        }

        .bold {
            font-weight: bold;
            
        }
        .button {
            display: inline-block;
            padding: 10px 20px;
            background-color: #AD88C6;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s;
        }

        .button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
     <!-- Container for the tax suggestions -->
    <div class="suggestions">
        <h2>Tax Calculation Suggestions</h2>
        <!-- Handlebars template for rendering suggestions conditionally -->
        {{#if suggestions.length}}
            <ul>
                <!-- Iterates over each suggestion and displays it as a list item -->
                {{#each suggestions}}
                    <li>{{this}}</li>
                {{/each}}
            </ul>
        {{else}}
        <!-- Displayed if there are no suggestions -->
            <p>No suggestions available.</p>
        {{/if}}
        <a href="/dashboard" class="button">Go to Dashboard</a>
    </div>
</body>
</html>
